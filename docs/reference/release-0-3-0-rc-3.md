# Repository tooling 0.3.0-rc.3

The third release candidate pins patched `sharp` and `smol-toml` transitive dependencies through
pnpm workspace overrides. The source repository and every generated repository profile carry the
same security constraint.

No stable release is associated with this candidate. Consumer validation determines whether the
package set is ready for promotion.
