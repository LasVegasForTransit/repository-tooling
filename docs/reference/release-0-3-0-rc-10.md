# Web platform 0.3.0-rc.10

This candidate makes Playwright screenshot baselines portable across developer machines and Linux
CI. The shared snapshot template separates desktop and mobile projects without embedding the host
operating system in committed filenames.

It includes the accessibility baseline introduced in `v0.3.0-rc.10`.
