# Web platform 0.3.0-rc.12

This candidate separates Playwright screenshot baselines by browser project and host rendering
platform. It prevents operating-system font metrics from weakening or invalidating visual checks.

It includes the accessibility baseline and Playwright snapshot directory support introduced in the
preceding candidates.
