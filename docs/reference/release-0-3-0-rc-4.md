# Repository tooling 0.3.0-rc.4

The fourth release candidate adds idempotent resources for Worker custom domains, Web Analytics
sites and tokens, and required GitHub environment secrets. Provider conflicts stop reconciliation
before any write.

Generated repositories retain the audited pnpm overrides and isolated Git hook behavior validated in
earlier candidates. No stable release is associated with this candidate.
