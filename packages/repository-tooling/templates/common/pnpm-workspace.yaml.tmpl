packages:
  - 'apps/*'
  - 'packages/*'
  - 'tools/*'

# The organization version baseline, written by @lvbt/repository-tooling.
# Every LVBT repository carries the same catalog, so a package declares
# "catalog:" and two repositories cannot drift onto different versions of one
# tool without it showing up as a change to this file.
catalog:
{{catalog}}
