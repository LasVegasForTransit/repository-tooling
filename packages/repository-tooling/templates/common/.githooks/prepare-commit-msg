#!/usr/bin/env sh
# Generated by @lvbt/repository-tooling; this repository owns this file.
# Adds a Co-Authored-By footer when a coding agent is driving the commit.
set -eu
ROOT=$(git rev-parse --show-toplevel)
sh "$ROOT/node_modules/@lvbt/repository-tooling/hooks/prepare-commit-msg.sh" "$@"
