#!/usr/bin/env sh
# Generated by @lvbt/repository-tooling; this repository owns this file.
# Runs `pnpm check` before anything leaves the laptop. CI is still the authority.
set -eu
ROOT=$(git rev-parse --show-toplevel)
sh "$ROOT/node_modules/@lvbt/repository-tooling/hooks/pre-push.sh" "$@"
