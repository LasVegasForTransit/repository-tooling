#!/usr/bin/env sh
# Generated by @lvbt/repository-tooling; this repository owns this file.
# The shared check validates the subject grammar and the scopes listed in
# .lvbt/commit-scopes.txt. Repository-specific rules go below it.
set -eu
ROOT=$(git rev-parse --show-toplevel)
sh "$ROOT/node_modules/@lvbt/repository-tooling/hooks/commit-msg.sh" "$@"
