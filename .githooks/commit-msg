#!/usr/bin/env sh
# This repository consumes its own standard: the same hook a generated
# repository gets, pointed at the package source instead of node_modules.
set -eu
ROOT=$(git rev-parse --show-toplevel)
sh "$ROOT/packages/repository-tooling/hooks/commit-msg.sh" "$@"
