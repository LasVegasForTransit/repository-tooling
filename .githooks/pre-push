#!/usr/bin/env sh
# Run the same serial check contributors use before this source of shared
# tooling publishes a policy other repositories would inherit.
set -eu
cd "$(git rev-parse --show-toplevel)"
pnpm check
